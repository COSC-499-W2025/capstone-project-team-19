# Virtual environment
venv/

# Python cache
__pycache__/
*.pyc

# Env variables and secrets
.env
*.env

# OS junk (mac and windows, to avoid any excess files being pushed)
.DS_Store
Thumbs.db

# Editor settings (just in case)
.vscode/
.idea/

# Build outputs (auto-generated every time we build, we should avoid committing them)
build/
dist/

# SQLite database and WAL files
*.db
*.db-shm
*.db-wal

# User's uploaded files (so that it is not accessible through our public github repo)
zip_data/

# Google Drive Auth
token.json
credentials.json

# DOCX Output
out/